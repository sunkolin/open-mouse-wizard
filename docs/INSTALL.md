# 游戏修改器 - GUI版本安装说明

## 概述
本项目已从Web界面迁移到GUI界面，使用Fyne框架开发。

## Windows系统要求

### 1. 安装GCC编译器

Fyne框架在Windows上需要GCC编译器才能正常工作。你可以选择以下任一方式安装：

#### 选项A：安装MinGW-w64（推荐）
1. 下载MinGW-w64：https://sourceforge.net/projects/mingw-w64/
2. 运行安装程序，选择合适的架构（x86_64用于64位系统）
3. 将MinGW的bin目录添加到系统PATH环境变量中
   - 默认路径通常是：`C:\Program Files\mingw-w64\x86_64-xxx-mingw32\bin`

#### 选项B：安装TDM-GCC
1. 下载TDM-GCC：https://jmeubank.github.io/tdm-gcc/
2. 运行安装程序
3. 安装程序会自动配置PATH环境变量

#### 选项C：使用MSYS2
1. 下载MSYS2：https://www.msys2.org/
2. 安装后打开MSYS2终端
3. 运行：`pacman -S mingw-w64-x86_64-gcc`
4. 将MSYS2的mingw64 bin目录添加到PATH

### 2. 验证GCC安装
打开新的PowerShell或CMD窗口，运行：
```powershell
gcc --version
```

如果显示GCC版本信息，说明安装成功。

## 编译和运行

### 方法1：直接运行（已编译版本）
如果已经有编译好的exe文件，直接双击运行即可。

### 方法2：从源代码编译
1. 确保已安装Go语言环境（Go 1.21+）
2. 确保已安装GCC编译器（见上文）
3. 在项目目录运行：
```powershell
go mod tidy
go build -o game-modifier.exe
```

4. 运行程序：
```powershell
.\game-modifier.exe
```

## 使用说明

### 基本操作流程

1. **选择进程**
   - 输入目标进程的完整名称（例如：notepad.exe）
   - 点击"查找进程"按钮

2. **搜索数值**
   - 首次搜索：输入游戏中的当前数值，点击"首次搜索"
   - 改变游戏中的数值
   - 再次搜索：输入新的数值，点击"再次搜索"来缩小范围
   - 重复直到找到少量地址

3. **修改数值**
   - 从地址列表中点击想要的地址（会自动填入地址框）
   - 输入想要修改的新值
   - 点击"修改值"按钮

### 注意事项
- ⚠️ **必须以管理员身份运行**：才能访问其他进程的内存
- 💡 **多次搜索技巧**：首次搜索会得到很多结果，通过改变游戏数值并多次搜索来精确定位
- 🎯 **地址选择**：通常选择稳定的地址（多次搜索都存在的）

## 故障排除

### 问题1：编译时提示"build constraints exclude all Go files"
**解决方案**：安装GCC编译器（见上文）

### 问题2：运行时提示"访问被拒绝"
**解决方案**：右键点击程序，选择"以管理员身份运行"

### 问题3：找不到进程
**解决方案**：
- 确保进程名称完整且正确（包括.exe扩展名）
- 确保目标进程正在运行
- 尝试使用任务管理器查看准确的进程名称

### 问题4：搜索不到任何地址
**解决方案**：
- 确认数值类型正确（当前仅支持32位整数）
- 尝试不同的数值
- 确保进程已正确打开

## 技术栈
- Go 1.24+
- Fyne v2.5.0（GUI框架）
- Windows API（内存操作）

## 免责声明
本程序仅用于学习和研究目的，请勿用于非法用途。
